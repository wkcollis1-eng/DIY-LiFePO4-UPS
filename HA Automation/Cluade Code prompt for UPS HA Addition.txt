Change type: SENSOR + AUTOMATION + DOCUMENTATION
Impacted files: configuration.yaml, automations.yaml, scripts.yaml, CLAUDE.md

Task: Integrate UPS v4 config into this repository.
Source file: ups_ha_config_v4.yaml (in repo root — read it before any edits)
Reference: HA_AUTOMATION_README.md (entity IDs and architecture)

Steps:
1. grep configuration.yaml automations.yaml scripts.yaml for any existing ups_* entity IDs — report hits before touching anything
2. Append sensor and template blocks from ups_ha_config_v4.yaml into configuration.yaml
3. Append automations from ups_ha_config_v4.yaml into automations.yaml
4. Append script.ups_graceful_shutdown into scripts.yaml
5. Add all new UPS entity IDs to §ENTITIES in CLAUDE.md under new group UPS
6. Run python3 tools/analyze_ha.py — zero hard errors required
7. Output diff summary only — no full file rewrites

Constraints:
- Do not modify any existing HVAC sensors or automations
- Preserve all inline comments from ups_ha_config_v4.yaml
- Kasa entity ID placeholder: switch.kasa_smart_plug_energy_monitoring — add note in CLAUDE.md that this needs confirming against actual entity registry
- Notify placeholder: notify.mobile_app_your_phone — same
- The Shelly typo (voltge) is intentional — do not correct it